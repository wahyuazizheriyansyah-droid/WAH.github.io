let lang = "en";

function toggleLang() {
    lang = (lang === "en") ? "id" : "en";

    document.querySelectorAll("[data-en]").forEach(el => {
        el.innerText = el.getAttribute(`data-${lang}`);
    });
}

/* Modal content */
/* Modal content / Database Modul */
const data = {
    fft: {
        en: "Processing and analyzing signal frequency spectrums using Fast Fourier Transform (FFT) in MATLAB.",
        id: "Memproses dan menganalisis spektrum frekuensi sinyal menggunakan Fast Fourier Transform (FFT) di MATLAB."
    },
    drone: {
        en: "End-to-end design and mechanical development of custom drone systems for mapping, GCS integration, and autonomous missions. Proven track record in national competitions (KRTI).",
        id: "Desain menyeluruh dan pengembangan mekanik sistem drone kustom untuk pemetaan, integrasi GCS, dan misi otonom. Terbukti dalam kompetisi nasional (KRTI)."
    },
    iot: {
        en: "Developing Internet of Things (IoT) systems equipped with custom PCB designs for telemetry, remote sensing, and efficient hardware integration.",
        id: "Mengembangkan sistem Internet of Things (IoT) yang dilengkapi dengan desain PCB kustom untuk telemetri, penginderaan jarak jauh, dan integrasi perangkat keras."
    }
};

function openModal(key) {
    document.getElementById("modal").style.display = "block";
    document.getElementById("modal-body").innerText = data[key][lang];
}

function closeModal() {
    document.getElementById("modal").style.display = "none";
}
// Tambahkan ini di bagian paling bawah script.js kamu
window.onload = () => {
    // Menjalankan fungsi update teks pertama kali
    document.querySelectorAll("[data-en]").forEach(el => {
        el.innerText = el.getAttribute(`data-${lang}`);
    });
};

// Tambahan interaksi suara kecil saat klik (Opsional)
function openModal(key) {
    document.getElementById("modal").style.display = "block";
    document.getElementById("modal-body").innerText = data[key][lang];
    console.log("Astra: Mengakses modul " + key + "...");
}