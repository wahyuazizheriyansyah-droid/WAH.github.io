let lang = "en";

function toggleLang() {
    lang = (lang === "en") ? "id" : "en";
    document.querySelectorAll("[data-en]").forEach(el => {
        el.innerText = el.getAttribute(`data-${lang}`);
    });
}

const data = {
    drone: {
        en: "End-to-end design of drone mechanics and GCS for mapping and KRTI missions.",
        id: "Desain menyeluruh mekanik drone dan GCS untuk pemetaan dan misi KRTI."
    },
    env_mon: {
        en: "Developing an IoT system to monitor air pollutants, temperature, and humidity, accessible through a mobile interface.",
        id: "Mengembangkan sistem IoT untuk memonitor polutan udara, suhu, dan kelembapan yang dapat diakses melalui HP."
    },
    soil_iot: {
        en: "An IoT solution that monitors soil conditions to classify them into productive or non-productive categories.",
        id: "Solusi IoT yang memantau kondisi tanah untuk mengklasifikasikannya menjadi kategori produktif atau non-produktif."
    },
    opamp: {
        en: "Designing Op-Amp circuits to transform square waves into triangle waves for signal processing.",
        id: "Mendesain rangkaian Op-Amp untuk mengubah gelombang segi empat menjadi segitiga untuk pemrosesan sinyal."
    },
    fft: {
        en: "Analyzing signal frequency spectrums using FFT algorithms in MATLAB.",
        id: "Menganalisis spektrum frekuensi sinyal menggunakan algoritma FFT di MATLAB."
    }
};

function openModal(key) {
    document.getElementById("modal").style.display = "block";
    document.getElementById("modal-body").innerText = data[key][lang];
}

function closeModal() {
    document.getElementById("modal").style.display = "none";
}

window.onload = toggleLang; // Menjalankan inisialisasi bahasa

function openModal(key) {
    document.getElementById("modal").style.display = "block";
    document.getElementById("modal-body").innerText = data[key][lang];
}

function closeModal() {
    document.getElementById("modal").style.display = "none";
}
// Tambahkan ini di bagian paling bawah script.js kamu
window.onload = () => {
    // Menjalankan fungsi update teks pertama kali
    document.querySelectorAll("[data-en]").forEach(el => {
        el.innerText = el.getAttribute(`data-${lang}`);
    });
};

// Tambahan interaksi suara kecil saat klik (Opsional)
function openModal(key) {
    document.getElementById("modal").style.display = "block";
    document.getElementById("modal-body").innerText = data[key][lang];
    console.log("Astra: Mengakses modul " + key + "...");
}